import { CambiarPasswordForm } from '@/components/cambiar-password-form';

export default function CambiarPasswordPage() {
  return (
    <CambiarPasswordForm
      title="Cambia tu contrasena"
      description="Por seguridad, tenes que definir una contrasena propia antes de continuar"
    />
  );
}
